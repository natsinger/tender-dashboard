# Data processing
pandas==2.3.3
plotly==6.5.2
requests==2.32.5
python-dateutil>=2.8.0

# Configuration
python-dotenv>=1.0.0

# Persistent user data storage
supabase==2.15.2

# PDF extraction
pdfplumber==0.11.9
Pillow==12.1.0

# OCR for scanned PDFs (optional — graceful degradation if not installed)
pytesseract==0.3.13
pypdfium2==4.36.0
opencv-python==4.13.0.92
ocrmypdf==17.4.0  # Requires Ghostscript on the system

# Mavat client (browser automation)
playwright==1.58.0
