/**
 * Tailwind class-name merge utility.
 *
 * Combines clsx (conditional class names) with tailwind-merge
 * (deduplication of conflicting Tailwind classes).
 *
 * Re-exported from utils.ts for convenience. Either import path works:
 *   import { cn } from "@/lib/cn";
 *   import { cn } from "@/lib/utils";
 */

export { cn } from "./utils";
