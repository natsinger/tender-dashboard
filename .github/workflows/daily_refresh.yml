name: Daily Tender Refresh

on:
  schedule:
    # Run at 06:00 UTC (09:00 Israel time) every day
    - cron: "0 6 * * *"
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install requests pandas python-dotenv

      - name: Fetch tenders and send alerts
        env:
          SMTP_HOST: smtp.office365.com
          SMTP_PORT: "587"
          SMTP_USER: ${{ secrets.M365_EMAIL }}
          SMTP_PASSWORD: ${{ secrets.M365_PASSWORD }}
          SMTP_FROM: ${{ secrets.M365_EMAIL }}
          DASHBOARD_URL: ${{ secrets.DASHBOARD_URL }}
        run: python scripts/refresh_tenders.py

      - name: Commit and push snapshot
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add tenders_list_*.json data/tenders.db
          # Only commit if there are changes
          git diff --cached --quiet || git commit -m "data: daily tender snapshot $(date +%Y-%m-%d)"
          git pull --rebase
          git push
